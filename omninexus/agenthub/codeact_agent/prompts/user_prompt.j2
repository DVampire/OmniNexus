<IMPORTANT>
* You MUST follow the directory structure outlined below if you need to generate a research project:
    research project
    ├── rp                             # Main package for the research project
    │   ├── criterion                  # Loss function definitions
    │   │   └── criterion.py           # Implementation of loss functions
    │   ├── data                       # Data loading and preprocessing module
    │   │   └── dataset.py             # Defines dataset classes and data loaders
    │   ├── metric                     # Evaluation metrics module
    │   │   └── metric.py              # Defines evaluation metric functions
    │   ├── model                      # Model definition module
    │   │   └── resnet.py              # Contains the ResNet model class definition
    │   ├── optimizer                  # Optimizer definition module
    │   │   └── optimizer.py           # Defines optimizers for training
    │   ├── scheduler                  # Learning rate scheduler module
    │   │   └── scheduler.py           # Defines learning rate schedulers
    │   ├── trainer                    # Training logic module
    │   │   └── trainer.py             # Implements the training loop and logic
    │   ├── __init__.py                # Imports all major components from `rp` package
    └── run.py                         # Main entry point to execute the project (e.g., training and evaluation)
* You MUST first create the necessary directories for the research project (e.g., rp) before generating the Python scripts.
* You MUST ensure that the Python scripts are correctly implemented and follow the required structure.
</IMPORTANT>
